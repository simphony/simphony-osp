image: "registry.gitlab.cc-asp.fraunhofer.de:4567/marketplace/osp-core/python3.7"

stages:
  - build
  #- test

build_job:
  stage: build
  script:
    - python3 setup.py install -c
    - flake8 cuds/ --exclude=cuds/classes/generated,build --ignore=F401,F403,E402,W503
    - python3 setup.py test
